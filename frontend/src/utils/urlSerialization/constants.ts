/**
 * Constants for URL Serialization
 */

// Current serialization version
export const CURRENT_VERSION = 2;

// URL length limits (typical browser limit is 2048)
export const DEFAULT_MAX_URL_LENGTH = 2048;